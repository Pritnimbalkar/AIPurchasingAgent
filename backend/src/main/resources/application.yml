spring:
  datasource:
    url: jdbc:h2:mem:purchasing;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: create-drop
    defer-datasource-initialization: true
  h2.console.enabled: true
server.port: 8080
